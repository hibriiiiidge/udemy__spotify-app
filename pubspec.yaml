name: udemy__spotify_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  audioplayers: ^6.4.0
  dio: 5.4.3
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  flutter_layout_grid: 2.0.7
  freezed_annotation: 2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.7
  freezed: ^2.4.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
  assets:
    - .env
